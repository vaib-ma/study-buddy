# Solution Repository

Solutions are intentionally separate from question files. Every solution record must use the exact same `question_id` as its question record.

Required minimum fields for a solution:

- `question_id`
- `solution_type`
- `solution_text`
- `final_answer`

Optional fields can include `tips`, `common_mistakes`, and `key_concepts`.
